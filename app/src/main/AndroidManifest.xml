<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DouyinLiquidGlass"
        tools:targetApi="31">

        <!-- ============ Xposed / LSPosed module metadata ============ -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/module_description" />
        <!-- Minimum classic Xposed API level (api-82). LSPosed provides it. -->
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <!-- Scope hint shown in the LSPosed manager -->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
        <!-- Let LSPosed relocate our prefs to a world-readable path so the
             hook (running inside Douyin) can read them live. -->
        <meta-data
            android:name="xposedsharedprefs"
            android:value="true" />

        <activity
            android:name=".status.ModuleStatusActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
